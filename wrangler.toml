name = "mycloud"
main = "worker.js"
compatibility_date = "2024-01-01"

# KV 存储空间（存储用户信息、收藏夹）
[[kv_namespaces]]
binding = "KV_STORE"
id = "替换为你的KV命名空间ID"

# R2 存储桶（存储上传的文件）
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "替换为你的R2桶名"

# 环境变量
[vars]
ADMIN_PASSWORD = "替换为你的管理员密码"
